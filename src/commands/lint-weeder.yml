---
description: |
  Lint the project with Weeder

parameters:
  stack-yaml:
    description: "Path to stack.yaml file to use"
    type: string
    default: stack.yaml

steps:
  - run:
      name: Weeder
      environment:
        PARAM_STACK_YAML: <<parameters.stack-yaml>>
      command: <<include(scripts/weeder.sh)>>
